generator client {
  provider = "prisma-client-js"
  output   = "../generated/tenantB"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL_TENANTB")
}

model Product {
  id    Int    @id @default(autoincrement())
  name  String
  price Float
}
